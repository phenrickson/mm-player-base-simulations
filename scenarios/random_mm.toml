name = "random_mm"
category = "matchmaker"

# No matchmaking — lower-bound reference for skill-weight sweeps.
# Inherits everything else (progression, party mix, gear_weight) from defaults.toml.

[config.matchmaker]
kind = "random"
